好的，我理解你的 frustration——我们确实在同一个问题上反复尝试。现在让我重新梳理，直接给出最可能解决你问题的步骤，并解释为什么之前的尝试可能没生效。

问题根源（总结）
目录模式 正常工作是因为 PyInstaller 自动将 Tcl/Tk 文件收集到 _internal/_tcl_data 和 _internal/_tk_data，并由内置的 runtime hook 自动设置环境变量指向它们。

单文件模式 下，你手动添加了 tcl 目录（Tree(..., prefix='tcl')）并在代码中设置环境变量指向 tcl/tcl8.6。但错误信息显示 Tcl 确实去那个路径找了，却找不到 init.tcl —— 说明文件没有被正确打包进临时目录，或者路径层级不对。

可能原因：

Tree 没有生效：可能因为源路径错误、权限问题，或者 PyInstaller 缓存导致未正确添加。

环境变量设置被内置 runtime hook 干扰：但实际上错误信息中第一个路径就是你设置的，说明环境变量生效了，所以问题在于文件缺失。

Tcl 还需要其他文件：比如 tcl8.6 目录下可能缺少 init.tcl 本身（但源目录肯定有）。

明确解决方案（请严格按顺序操作）
步骤 1：确认源目录结构
在命令行执行以下命令，查看你的 Python Tcl 目录结构：

cmd
dir C:\Users\longyaosi\python-sdk\python3.13.2\tcl
你应该看到类似：

text
2025-...  <DIR>  tcl8.6
2025-...  <DIR>  tk8.6
确保 tcl8.6 下确实有 init.tcl。

步骤 2：修改 .spec 文件（关键修改）
将你的单文件 spec 修改为分别添加 tcl8.6 和 tk8.6 到正确的目标路径，以便匹配 PyInstaller 的 runtime hook 预期。移除 main.py 中手动设置环境变量的代码（让 runtime hook 自动处理）。

修改后的 离线网页下载器_单文件.spec（在 Analysis 之后、PYZ 之前添加以下代码）：

python
import os
import sys
from PyInstaller.utils.hooks import collect_data_files

# 你的原有 datas 保持不变
datas = [('assets/icon.ico', '.'), ('config.json', '.')]
# ... 其他原有内容

a = Analysis(['main.py'], ...)  # 保持原有 Analysis 不变

# ---------- 新增：添加 Tcl/Tk 数据文件（使用 Tree，分别添加到 _tcl_data 和 _tk_data）----------
tcl_base = os.path.join(sys.base_prefix, 'tcl')   # 例如 C:/.../python3.13.2/tcl
if os.path.exists(tcl_base):
    # 添加 tcl8.6 到 _tcl_data
    tcl86_path = os.path.join(tcl_base, 'tcl8.6')
    if os.path.exists(tcl86_path):
        a.datas += Tree(tcl86_path, prefix='_tcl_data')
    # 添加 tk8.6 到 _tk_data
    tk86_path = os.path.join(tcl_base, 'tk8.6')
    if os.path.exists(tk86_path):
        a.datas += Tree(tk86_path, prefix='_tk_data')
else:
    print("警告：未找到 Tcl 基础目录！")
# ---------------------------------------------------------------------------------

pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)
# ... 后面的 EXE 部分保持不变
步骤 3：移除 main.py 中的环境变量设置
将你在 main.py 开头添加的 set_tcl_env 函数及相关调用全部删除。因为 PyInstaller 内置的 runtime hook pyi_rth__tkinter.py 会自动设置 TCL_LIBRARY 和 TK_LIBRARY 指向 _tcl_data 和 _tk_data，只要这些目录存在且包含所需文件。

步骤 4：清理并重新打包
在项目目录下执行：

cmd
rmdir /s /q build dist
pyinstaller --clean "离线网页下载器_单文件.spec"
步骤 5：测试并验证
运行生成的单文件：

cmd
dist\离线网页下载器_单文件.exe
如果仍然出错，请在 main.py 开头（但要在任何导入之前）添加以下调试代码，查看临时目录中到底有什么文件：

python
import os
import sys

if getattr(sys, 'frozen', False):
    print("临时目录:", sys._MEIPASS)
    print("目录内容:", os.listdir(sys._MEIPASS))
    if '_tcl_data' in os.listdir(sys._MEIPASS):
        print("_tcl_data 内容:", os.listdir(os.path.join(sys._MEIPASS, '_tcl_data')))
    input("按回车继续...")
运行后截图或记录输出，这将直接告诉我们文件是否打包成功。

为什么这样改？
使用 _tcl_data 和 _tk_data：这是 PyInstaller 内置 hook 默认使用的路径，runtime hook 会自动设置环境变量指向它们，无需手动干预。

分别添加 tcl8.6 和 tk8.6：因为 Tcl 和 Tk 的库文件需要分别放在 TCL_LIBRARY 和 TK_LIBRARY 指向的目录下，如果混在一起可能导致找不到。

移除手动环境变量：避免与 runtime hook 冲突，简化问题。

如果还不行，可能的后备方案
降级 PyInstaller：尝试 pip install pyinstaller==5.13.2（一个较稳定版本），因为 6.x 对 Python 3.13 的支持可能还有 bug。

使用 Nuitka：编译成单个 exe 对 Tkinter 支持更好，但编译时间较长。

直接发布目录版：既然目录版可用，用 Inno Setup 等工具打包成安装程序，用户安装后使用，也是一种选择。

请先按上述步骤操作，这应该是目前最直接、最符合 PyInstaller 设计思路的解法。如果仍有问题，请提供调试输出的内容，我们继续分析。